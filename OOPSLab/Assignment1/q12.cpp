#include <bits/stdc++.h>
#define ll long long
#define vll vector<long long>
#define db double
#define vi vector<int>
using namespace std;

inline void add(int &a, int &b, int &c)
{
    cout << a + b + c << endl;
}

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    add(a, b, c);

    return 0;
}